{
  "name": "findoraddword",
  "version": "1.0.0",
  "description": "Write a method that takes in 2 parameters, a list of words and a search word. The method should determine whether or not the search word exists in the list. If the word is not found the list, add the word into the list and check that the list now contains the word.",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "john steckelberg",
  "license": "ISC"
}
